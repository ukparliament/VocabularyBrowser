@model Skos

@{
    ViewData["Title"] = Localizer["Title"];
}

<div class="section--primary">
    <div class="container">
        <h1>@Localizer["Title"]</h1>
    </div>
</div>

<section id="content">
    <div class="container">
        <ol class="list--block">
            @foreach (var scheme in Model.ConceptSchemes.OrderBy(x => x.PrefLabel.Single()))
            {
                <li>
                    <div class="card">
                        <div class="card__details">
                            <h2>
                                <a asp-action="Item" asp-route-id="@scheme.Id">@scheme.PrefLabel.Single()</a>
                            </h2>
                        </div>
                    </div>
                </li>
            }
        </ol>
    </div>
</section>
